# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: © 2026 Weaver Contributors
#
# 结构化输出（call_at 传入 output_model）时追加到 system_prompt 最末尾的 JSON-only
# 强制约束。加载方：core/llm/client.py。利用弱模型 recency bias（对末尾指令记忆最强）
# 统一兜底所有结构化 CallPoint，避免逐 prompt 文件重复。
#
# 用单行 basic string + \n 转义，避免 TOML 多行字符串吞掉紧跟 """ 的首个换行，
# 从而与历史硬编码值逐字节一致。

json_format_tail = "\n\n【输出格式·强制】\n仅输出一个 JSON 对象：首字符必须是 \"{\"，末字符必须是 \"}\"。\n禁止 ```代码块``` 围栏、禁止任何解释/前言/结语、禁止 JSON 以外任何文字。"
